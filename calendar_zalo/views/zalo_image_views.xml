<odoo>
    <record id="view_zalo_image_form" model="ir.ui.view">
        <field name="name">zalo.image.form</field>
        <field name="model">zalo.image</field>
        <field name="arch" type="xml">
            <form string="Zalo Image">
                <sheet>
                    <group>
                        <field name="name" required="1"/>
                        <field name="zalo_id"/>
                        <field name="local_file" widget="image"/>
                        <field name="image_url" readonly="1"/>
                        <field name="response_data" readonly="1"/>
                    </group>

                    <div class="oe_note mt16">
                        <p>
                            <strong>Lưu ý:</strong> File sau khi được upload và gửi đến người dùng sẽ được lưu trên server tối đa 7 ngày.
                            Trường hợp người dùng xem lại file sau khoảng thời gian trên sẽ được thông báo file không còn tồn tại.
                        </p>
                        <p>
                            <strong>Quota cho API là:</strong><br/>
                            - 5000 request/ ngày<br/>
                            - 5000 request/ tuần<br/>
                            - 5000 request/ tháng
                        </p>
                        <p>
                            <strong>Nội dung nhị phân của file ảnh:</strong><br/>
                            Các định dạng ảnh hỗ trợ: jpg và png<br/>
                            Dung lượng tối đa: 1MB<br/>
                        </p>
                    </div>

                </sheet>
            </form>
        </field>
    </record>

    <record id="view_zalo_image_tree" model="ir.ui.view">
        <field name="name">zalo.image.tree</field>
        <field name="model">zalo.image</field>
        <field name="arch" type="xml">
            <list string="Zalo Images">
                <field name="name"/>
                <field name="image_url"/>
            </list>
        </field>
    </record>

    <record id="action_zalo_image" model="ir.actions.act_window">
        <field name="name">Zalo Images</field>
        <field name="res_model">zalo.image</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_zalo_image" name="Hình ảnh zalo" parent="calendar.mail_menu_calendar" sequence="20" action="action_zalo_image"/>
</odoo>
